[phases.setup]
nixPkgs = ["python311", "gcc", "glib", "nss", "atk", "cups", "dbus"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "python -m playwright install-deps",
  "python -m playwright install chromium"
]

[start]
cmd = "python parsers/4399-4400/run.py"
